#!/bin/sh

set -e

SYMLINK="/usr/bin/usg"

if [ "${1:-}" = "remove" ] || [ "${1:-}" = "upgrade" ]; then
    rm -f -- "$SYMLINK"
fi

# Bazele de date, configurațiile și logurile utilizatorului sunt păstrate.
# Fișierele urmărite de pachet sunt eliminate automat de dpkg.
exit 0
